import { HistoryOperationDirective } from './history-operation.directive';

describe('HistoryOperationDirective', () => {
  it('should create an instance', () => {
    const directive = new HistoryOperationDirective();
    expect(directive).toBeTruthy();
  });
});
